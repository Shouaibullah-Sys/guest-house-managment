// components/slider/components/OtherInfo.tsx (if you still need it)
import React from "react";
import { Data } from "@/components/home/HeroSection";

type Props = {
  data: Data;
};

function OtherInfo({ data }: Props) {
  return null; // This component is replaced by the enhanced SlideInfo
}

export default OtherInfo;
